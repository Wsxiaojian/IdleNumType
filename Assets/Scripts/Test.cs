using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        NumType a = new NumType(10.0f,2);

        NumType b = a;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
